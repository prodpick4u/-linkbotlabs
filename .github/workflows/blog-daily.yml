name: Daily Blog Generator

on:
  workflow_dispatch:
  schedule:
    - cron: '0 4 * * *'  # 11:00 AM Thailand time

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Cache pip packages
        uses: actions/cache@v3
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: Set up Python 3.11
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          playwright install

      - name: Install FFmpeg (for moviepy)
        run: sudo apt-get update && sudo apt-get install -y ffmpeg

      - name: Run blog generator script
        run: python main.py

      - name: Run YouTube script generator
        run: python youtube_script_generator.py

      - name: Commit and push generated blog
        shell: bash
        run: |
          git config --global user.name "Auto Bot"
          git config --global user.email "bot@example.com"
          git add docs/
          if ! git diff --cached --quiet; then
            git commit -m "🤖 Auto-update blog"
            git push origin main
          else
            echo "No changes to commit"
